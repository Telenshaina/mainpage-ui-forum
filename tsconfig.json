{
  "compilerOptions": {
    "composite": true, // Required when using "references"
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"] // ✅ Adds support for @ imports
    }
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
